import React from 'react';

const Header = () =>
  <div>
    <div className='jumbotron jumbotron-fluid'>
      <div className='container'>
        <h1 className='display-5'>Remember the images!</h1>
        <p className='lead'>Click an image, but don't click the same one twice!</p>
      </div>
    </div>
  </div>;

export default Header;
